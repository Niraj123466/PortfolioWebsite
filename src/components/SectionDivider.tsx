// SectionDivider is now handled by border-t border-border on each section.
// This component is kept as a no-op for backwards compatibility.
const SectionDivider = (_props: { flip?: boolean }) => null;
export default SectionDivider;
